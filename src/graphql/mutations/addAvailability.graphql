mutation addAvailability(
  $userId: ID!
  $start: String!
  $end: String!
  $userType: UserType!
) {
  addAvailability(
    userId: $userId
    start: $start
    end: $end
    userType: $userType
  ) {
    id
    start
    end
  }
}
