mutation updateAvailability(
  $event_id: ID
  $user_id: ID!
  $start: String!
  $end: String!
) {
  updateAvailability(
    event_id: $event_id
    user_id: $user_id
    start: $start
    end: $end
  ) {
    start
    end
  }
}
