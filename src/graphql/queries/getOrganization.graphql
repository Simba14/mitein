query GetOrganization($id: ID, $userId: ID!, $locale: String!) {
  organization(id: $id, userId: $userId) {
    id
    city
    description(locale: $locale)
    logo
    name
    socials {
      facebook
      instagram
      twitter
    }
    tags
    website
  }
}
